## layout.js in app folder 
This is the template for your whole app. Every page will have the fonts loaded, the navbar visible,and this metadata applied.It's like the blueprint that all your other pages are placed into.

## {children}
It's like having a consistent page frame (layout.js) with a slot in the middle {children} that shows different content based on the route